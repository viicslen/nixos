# Package outputs for the NixVim flake
# This file is imported by flake.nix via flake-parts
{
  # Package outputs are defined in perSystem in flake.nix
  # This file exists for structural consistency with other flakes
  # and can be extended if more complex package definitions are needed
}
